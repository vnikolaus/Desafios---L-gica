const arr = [ [ 1, 0, 5 ], [ 1, 1, 7 ], [ 1, 0, 3 ], [ 2, 1, 0 ], [ 2, 1, 1 ] ]

dynamicArray(2, arr)

function dynamicArray(n, queries) {
    const array = new Array(n).fill(0)
    let lastAnswer = 0
    console.log(0 + 0 % 2);
}